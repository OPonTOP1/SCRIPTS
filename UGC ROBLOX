local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

-- إنشاء الـ GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
screenGui.Name = "TicketGUI"
screenGui.ResetOnSpawn = false

-- إنشاء الإطار الرئيسي
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 200, 0, 160)
frame.Position = UDim2.new(0.5, -100, 0.5, -80)
frame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui

-- إضافة زوايا مستديرة للإطار
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 15)
corner.Parent = frame

-- إضافة تأثير التوهج للإطار
local glow = Instance.new("UIStroke")
glow.Thickness = 2
glow.Color = Color3.fromRGB(0, 150, 255)
glow.Transparency = 0.3
glow.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
glow.Parent = frame

-- إضافة ظل للإطار
local shadow = Instance.new("Frame")
shadow.Size = UDim2.new(1, 8, 1, 8)
shadow.Position = UDim2.new(0, 4, 0, 4)
shadow.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
shadow.BackgroundTransparency = 0.6
shadow.BorderSizePixel = 0
shadow.ZIndex = -1
shadow.Parent = frame

local shadowCorner = Instance.new("UICorner")
shadowCorner.CornerRadius = UDim.new(0, 15)
shadowCorner.Parent = shadow

-- إنشاء الزر
local button = Instance.new("TextButton")
button.Size = UDim2.new(0.85, 0, 0.4, 0)
button.Position = UDim2.new(0.075, 0, 0.2, 0)
button.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
button.Text = "INSTANT UGC"
button.TextColor3 = Color3.fromRGB(255, 255, 255)
button.TextSize = 24
button.Font = Enum.Font.GothamBlack
button.TextStrokeTransparency = 0.5
button.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
button.Parent = frame

-- إضافة زوايا مستديرة للزر
local buttonCorner = Instance.new("UICorner")
buttonCorner.CornerRadius = UDim.new(0, 12)
buttonCorner.Parent = button

-- إضافة تأثير التوهج للزر
local buttonGlow = Instance.new("UIStroke")
buttonGlow.Thickness = 2
buttonGlow.Color = Color3.fromRGB(100, 200, 255)
buttonGlow.Transparency = 0.2
buttonGlow.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
buttonGlow.Parent = button

-- إضافة ظل للزر
local buttonShadow = Instance.new("Frame")
buttonShadow.Size = UDim2.new(1, 4, 1, 4)
buttonShadow.Position = UDim2.new(0, 2, 0, 2)
buttonShadow.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
buttonShadow.BackgroundTransparency = 0.7
buttonShadow.BorderSizePixel = 0
buttonShadow.ZIndex = -1
buttonShadow.Parent = button

local buttonShadowCorner = Instance.new("UICorner")
buttonShadowCorner.CornerRadius = UDim.new(0, 12)
buttonShadowCorner.Parent = buttonShadow

-- إضافة النص للحقوق
local creditLabel = Instance.new("TextLabel")
creditLabel.Size = UDim2.new(0.85, 0, 0.25, 0)
creditLabel.Position = UDim2.new(0.075, 0, 0.7, 0)
creditLabel.BackgroundTransparency = 1
creditLabel.Text = "Credit by: Zz | Scripter"
creditLabel.TextColor3 = Color3.fromRGB(200, 200, 255)
creditLabel.TextSize = 16
creditLabel.Font = Enum.Font.GothamBold
creditLabel.Parent = frame

-- جعل الـ GUI متجاوبًا مع الهواتف
local function adjustGuiForMobile()
    if UserInputService.TouchEnabled then
        frame.Size = UDim2.new(0, 160, 0, 130)
        frame.Position = UDim2.new(0.5, -80, 0.5, -65)
        button.TextSize = 20
        creditLabel.TextSize = 14
    end
end
adjustGuiForMobile()

-- إضافة أنيميشن التكبير عند التشغيل
frame.Size = UDim2.new(0, 0, 0, 0)
local tweenInfo = TweenInfo.new(
    0.6,
    Enum.EasingStyle.Back,
    Enum.EasingDirection.Out,
    0,
    false,
    0
)
local tween = TweenService:Create(frame, tweenInfo, {Size = UDim2.new(0, 200, 0, 160)})
tween:Play()

-- إضافة تأثير النبضة للتوهج
local pulseTween = TweenService:Create(glow, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {Transparency = 0.1})
pulseTween:Play()

-- إضافة تأثيرات التفاعل للزر
local originalSize = button.Size
local originalColor = button.BackgroundColor3

button.MouseEnter:Connect(function()
    local hoverTween = TweenService:Create(button, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection Out), {
        Size = UDim2.new(originalSize.X.Scale * 1.05, originalSize.X.Offset, originalSize.Y.Scale * 1.05, originalSize.Y.Offset),
        BackgroundColor3 = Color3.fromRGB(0, 140, 255)
    })
    hoverTween:Play()
end)

button.MouseLeave:Connect(function()
    local leaveTween = TweenService:Create(button, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection Out), {
        Size = originalSize,
        BackgroundColor3 = originalColor
    })
    leaveTween:Play()
end)

button.MouseButton1Down:Connect(function()
    local clickTween = TweenService:Create(button, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection Out), {
        Size = UDim2.new(originalSize.X.Scale * 0.95, originalSize.X.Offset, originalSize.Y.Scale * 0.95, originalSize.Y.Offset)
    })
    clickTween:Play()
end)

button.MouseButton1Up:Connect(function()
    local releaseTween = TweenService:Create(button, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection Out), {
        Size = originalSize
    })
    releaseTween:Play()
end)

-- إضافة وظيفة الزر
button.MouseButton1Click:Connect(function()
    local args = {
        "KidzBop2025",
        "2" -- سيتم تغيير هذا الرقم في كل مرة
    }

    while true do
        for i = 1, 2000 do
            args[2] = tostring(i) -- تحويل الرقم إلى نص
            game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Services"):WaitForChild("CollectibleServiceRemotes"):WaitForChild("Collect"):InvokeServer(unpack(args))
            
            wait(0.1) -- تأخير لتجنب الحظر (يمكنك تعديله)
        end
    end
end)
